FROM postgres:15-alpine

# Set default environment variables for the database
ENV POSTGRES_DB=ai_summarizer
ENV POSTGRES_USER=postgres
ENV POSTGRES_PASSWORD=postgres

# Expose the default PostgreSQL port
EXPOSE 5432
